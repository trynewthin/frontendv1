version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vue-app
    ports:
      - "8020:8020"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    restart: unless-stopped 